<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name="com.dungeon.DungeonApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light">

        <!-- Intro Activity (Launch Screen) -->
        <activity
            android:name="com.dungeon.ui.IntroActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Game Activity -->
        <activity
            android:name="com.dungeon.ui.BattleActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Inventory Activity -->
        <activity
            android:name="com.dungeon.ui.InventoryActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Team Activity -->
        <activity
            android:name="com.dungeon.ui.TeamActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.NoActionBar" />

        <!-- Widget Dialog Activity -->
        <activity
            android:name="com.dungeon.ui.WidgetDialogActivity"
            android:theme="@style/Theme.Transparent.Dialog"
            android:exported="false"
            android:excludeFromRecents="true"
            android:taskAffinity="" />

        <!-- Main Activity (Placeholder) -->
        <activity
            android:name="com.dungeon.ui.MainActivity"
            android:exported="false" />

        <!-- Widget Provider -->
        <receiver
            android:name="com.dungeon.widget.DungeonWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/dungeon_widget_info" />
        </receiver>
    </application>
</manifest>